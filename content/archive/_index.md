---
title: "Archive"
sidebar:
  exclude: false   # this page stays in the menu; the `cascade` in this file hides only the pages inside it
description: "Past newsletters, chairman's reports, minutes and leaflets from Friends of Telford Town Park."
# Each document is a small folder (its details plus the PDF). The list on this page links straight to the
# PDF, so the documents don't get pages of their own. The `build` lines just below are for this page itself
# (the `cascade` would otherwise switch it off too); the `cascade` is for the documents.
build:
  render: always
  list: always
cascade:
  build:
    render: never
    list: local
  # Keep the 44 documents out of the phone menu, which should list only the site's main pages.
  sidebar:
    exclude: true
---

Past newsletters, reports and other documents from Friends of Telford Town Park, shown as they were published. They come from our old website, so the collection isn't complete. To protect people's privacy, individuals' private email addresses and phone numbers have been blacked out.

Looking for something that isn't here? [Get in touch](/contact-us/) and we'll see if we can find it.
